# git lab language server

- install it as an LSP
- comments on a file will show as diagnostics (hints for resolved, info for unresolved)
  - code actions to reply/resolve/add new comment
- diagnostic at top of file for all changed files (use workspace diagnostic picker to see all)
- uses `glab` for auth, so you'll need that installed
